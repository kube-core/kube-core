cloud:
  project: myproject
  provider: gcp

cluster:
  registries:
    self:
      enabled: true
      cleanup:
        schedule: "0 9 * * 1"
    dockerhub:
      enabled: false
    catalogue:
      enabled: false
    extraRegistries: []
