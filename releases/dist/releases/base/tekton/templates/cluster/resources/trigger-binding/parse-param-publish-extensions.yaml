apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: parse-param-publish-extensions
  namespace: tekton-pipelines
spec:
  params:
  - name: publish
    value: $(extensions.publish)