apiVersion: v1
kind: Service
metadata:
  name: {{ include "flux-repository.fullname" . }}
  labels:
    {{- include "flux-repository.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flux-repository.selectorLabels" . | nindent 4 }}
