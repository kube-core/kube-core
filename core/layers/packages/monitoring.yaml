releases:
  prometheus-pushgateway:
    labels:
      package: monitoring
    enabled: true
  kps:
    labels:
      package: monitoring
    enabled: true
  # Not used that much. Probably should be in a monitoring-extras package instead ?
  # sloop:
  #   labels:
  #     package: monitoring
  #   enabled: true
